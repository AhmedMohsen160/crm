#!/bin/sh
# ═══════════════════════════════════════════════════════════════
#  يُنفَّذ عند كل تشغيل للنظام على السيرفر:
#   1) يجهّز جداول قاعدة البيانات (آمن ويعمل في كل مرة)
#   2) يُنشئ حساب مدير النظام إن لم يكن موجودًا
#   3) يشغّل التطبيق
# ═══════════════════════════════════════════════════════════════
set -e

echo "→ تجهيز جداول قاعدة البيانات..."
npx prisma db push --skip-generate --accept-data-loss

echo "→ التأكد من وجود حساب مدير النظام..."
node ./scripts/ensure-admin.mjs

echo "✓ جاهز — تشغيل النظام"
exec "$@"
