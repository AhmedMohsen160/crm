# تقرير الفجوة — منظومة فاست ترانس مقابل الـCRM القائم

> أُنتج قبل كتابة أي كود، التزامًا بـ§٠ من مواصفة التطوير.
> المرجع: `مواصفة تطوير منظومة فاست ترانس على الـCRM القائم`.

**الخلاصة في سطر:** القائم هو CRM مبيعات عام سليم البنية، يغطي **الليدز والعملاء
والتسعير** جزئيًا، لكنه **لا يحتوي كائن «مشروع» تشغيليًا، ولا أي طبقة تكلفة أو
إنتاج أو فريلانسرز**، وصلاحياته ثلاثة أدوار مشفّرة في الكود بدل ١٨ صلاحية كبيانات.

| التصنيف | عدد البنود |
|---|:-:|
| ✅ موجود ومطابق | ١٤ |
| 🟡 موجود ويحتاج تعديلًا | ٢١ |
| ❌ غير موجود | ٣٨ |

---

## ١. الجداول

### 1.1 `clients` — العملاء

**الحالة: 🟡 موجود ويحتاج تعديلًا جوهريًا (مفكّك على جدولين)**

القائم يفصل `Company` (`prisma/schema.prisma:49`) عن `Contact` (`:84`)، بينما
المواصفة تريد كائن `clients` واحدًا يخدم الفرد والشركة معًا (§4.1).

| حقل المواصفة | الحالة | الموقع / الملاحظة |
|---|---|---|
| `client_id` تسلسلي `CL-00001` | ❌ | القائم يستخدم `cuid()` عشوائيًا — `schema:85` |
| `name` | ✅ | `Contact.firstName/lastName` `:86-87` |
| `phone` **مطبَّع** ومفتاح تكرار | 🟡 | الحقل موجود `:89-90` لكن **بلا تطبيع وبلا منع ازدواج** |
| `type` فرد/شركة | ❌ | مستنتج ضمنًا من وجود `companyId` |
| `company_name`, `tax_id` | ✅ | `Company.name:50`، `Company.taxNumber:60` |
| `country`, `city` | 🟡 | على `Company` فقط `:57-58`، ليست على الفرد |
| `first_branch` | ❌ | لا يوجد مفهوم فرع في النظام إطلاقًا |
| `created_by` | ❌ | يوجد `ownerId` لا مُنشئ |

**القاعدة الإلزامية «لا عميل مكرر أبدًا» (§4.1): ❌ غير مطبَّقة.**
البحث الحالي في `src/app/(app)/contacts/page.tsx:26-33` بحث نصي عادي، ولا يمنع
إنشاء عميل بهاتف موجود.

### 1.2 `leads` — الليدز

**الحالة: 🟡 موجود ويحتاج تعديلًا**

`Lead` في `schema:118`.

| حقل المواصفة | الحالة | الملاحظة |
|---|---|---|
| `lead_id` بصيغة `LD-2608-0001` | ❌ | `cuid()` — `:119` |
| `created_at` **بالساعة** | ✅ | `:134` DateTime كامل |
| `channel` | 🟡 | `source` `:124` — القائمة الحالية لا تطابق قنوات §٩ |
| `contact_method` | ❌ | |
| `sales_admin`, `branch` | 🟡 | `ownerId` `:136` موجود، **`branch` غير موجود** |
| `stage` (٦ مراحل) | 🟡 | `status` `:125` بخمس قيم مختلفة الأسماء |
| **`first_reply_at`** | ❌ | **حرج** — عليه يقوم قياس سرعة الرد و SLA |
| `closed_at` | 🟡 | `convertedAt` `:132` عند الفوز فقط، لا عند الخسارة |
| `loss_reason` **إلزامي عند الخسارة** | ❌ | لا حقل ولا إلزام |
| `requested_service`, `est_pages` | 🟡 | `serviceInterest:127` موجود، `est_pages` غير موجود |

### 1.3 `projects` — المشاريع (الجدول المحوري)

**الحالة: 🟡 هيكل جزئي في `Deal` (`schema:151`) — ينقصه أكثر من نصف حقوله وكل طبقة التكلفة**

**موجود ويمكن ترحيله:**

| المواصفة | القائم |
|---|---|
| `client_id` | `companyId:174` + `contactId:177` |
| `lead_id` | ❌ (الليد يُحوَّل ولا يُربط) |
| `service_line` | `serviceType:161` — القائمة لا تطابق §٩ |
| `lang_from/to` | `sourceLang/targetLang:162-163` ✅ |
| `pages` | `pageCount:165` 🟡 (اختياري، والمواصفة تجعله إلزاميًا) |
| `net_total` | `amount:156` 🟡 (مُدخل يدويًا لا محسوبًا) |
| `currency` | `currency:157` ✅ |
| `deadline` | `deliveryDate:166` ✅ |
| `sales_admin` | `ownerId:180` ✅ |

**غير موجود إطلاقًا (❌):**

`project_id` بصيغة `PR-2608-0042` · `unit_price` من قائمة الأسعار ·
`is_rush` · `discount_type` · `discount_value` · `gross` · `deposit` ·
`collected_amount` · `collected_at` · `balance` · **`is_express`** ·
`branch` · `approval_state` · `approved_by` · **`project_manager`** ·
**`work_mode`** · `sourcing` · **`primary_producer`** · `reviewer` ·
`qa_issues` · `is_rework` · `folder_url` · `converted_at` · `assigned_at` ·
`delivered_at` · **`weighted_pages`** · **`cost_internal`** ·
**`cost_external`** · **`cost_total`** · **`margin`** · `margin_pct` ·
`revenue_month` · **حقل `alert`**

**حالات المشروع:** القائم عنده ٥ مراحل بيعية (`NEW→QUOTED→NEGOTIATION→WON→LOST`،
`src/lib/constants.ts:19-25`) وهي **مراحل ليد لا مراحل تشغيل**. المواصفة تريد
٦ حالات تشغيلية + حالتين جانبيتين (§٦). **تعارض بنيوي يحتاج قرارًا** — انظر §٥ أدناه.

### 1.4 `project_steps` — خطوات التنفيذ

**❌ غير موجود بالكامل.** لا جدول ولا مفهوم.

### 1.5 `users` / `roles` / `staff_cost`

**`users`: 🟡** — `schema:19`

| المواصفة | الحالة |
|---|---|
| `name`, `username`, `password_hash`, `status` | ✅ `:20,22,24,27` |
| `role` | 🟡 نص حر بثلاث قيم `:23` |
| `branch` | ❌ |
| **`reports_to`** | ❌ — **بدونه لا يوجد «نطاق الفريق» إطلاقًا** |
| **`is_producer`** | ❌ |
| `last_login` | ❌ |

**`roles`: ❌ غير موجود.** الأدوار اليوم ثابتة في الكود:
`src/lib/constants.ts:6-10` ثلاثة أدوار، والفحص يتم بالدور لا بالصلاحية:
`src/lib/auth.ts:104-106` (`canSeeAll`) و`:113` (`isAdmin`).
**هذا يخالف §٥ صراحةً** («افحص الصلاحية لا الدور»).

**`staff_cost`: ❌ غير موجود.** لا رواتب ولا `productive_ratio` ولا `daily_capacity`.

### 1.6 الفريلانسرز

**❌ الثلاثة غير موجودة:** `freelancers` · `freelancer_rates` · `freelancer_payments`.

### 1.7 الجداول المساندة

| الجدول | الحالة | الملاحظة |
|---|---|---|
| `price_list` | 🟡 | `PriceRate` `:343` — **ينقصه `effective_from` و`min_order`**، فتغيير السعر اليوم **يغيّر أسعار الماضي** (مخالفة §4.7) |
| `settings` | 🟡 | `Setting` `:337` مفتاح/قيمة موجود لكن **بلا أي شاشة إدارة وبلا أي مفتاح مستخدم فعليًا** |
| `lists` | ❌ | القوائم كلها مشفّرة في `src/lib/constants.ts:105-160` — تخالف §٩ («لا تُشفّرها في الكود») |
| `audit_log` | 🟡 | `Activity` `:311` يسجّل أحداثًا وصفية، **لا يسجّل الحقل ولا القيمة القديمة/الجديدة** |

### 1.8 جداول لا مقابل لها في المواصفة

| القائم | القرار المقترح |
|---|---|
| `Quote` + `QuoteItem` `:257,289` | **يُبقى** — عرض السعر أداة مبيعات مفيدة، والمواصفة تذكر «صلاحية عرض السعر» (§١٠). يُربط بالمشروع. |
| `Task` `:197` | **يُبقى** — لا يعارض شيئًا ويفيد المتابعة |
| `Note` `:233` | **يُبقى** |

---

## ٢. الصلاحيات

**الحالة: ❌ الفجوة الأكبر والأخطر.**

| المواصفة | القائم |
|---|---|
| ١٨ صلاحية منفصلة | ٣ أدوار مشفّرة |
| ٧ أدوار | ٣ (`ADMIN`/`MANAGER`/`AGENT`) `constants.ts:6-10` |
| الأدوار **بيانات** تُنشأ من الشاشة | ثوابت في الكود |
| فحص الصلاحية `can(user,'x')` | فحص الدور `user.role === 'ADMIN'` `auth.ts:113` |
| نطاق **الفريق** عبر `reports_to` | ❌ لا يوجد — الرؤية ثنائية: الكل أو الذات `auth.ts:104-110` |
| حجب الحقول **من الخادم** | ❌ — لا يوجد حقل محجوب أصلًا لعدم وجود تكاليف |
| مؤشر التكلفة المجرّد | ❌ |

**الصلاحيات الـ١٨ كلها غير موجودة**، ولا واحدة منها مطبَّقة بأي صورة.

---

## ٣. الشاشات

| شاشة المواصفة | الحالة | الموقع / الفجوة |
|---|---|---|
| **ليد جديد — الهاتف أولًا** | 🟡 | `leads/new/page.tsx` — النموذج ١٢ حقلًا والهاتف ليس أولًا ولا بحث فوري. المعيار ≤١٥ث بثلاثة حقول |
| **تحويل ليد → مشروع** | 🟡 | `leads/[id]/convert/page.tsx` — ينشئ شركة+جهة+صفقة، لكن بلا تسعير تلقائي ولا مقدم ولا زر «فوري» |
| بحث العميل بالهاتف | ❌ | لا شاشة تعرض تاريخ العميل وقيمته |
| **التحصيل** | ❌ | لا مفهوم تحصيل إطلاقًا |
| **لوحتي** (المبيعات) | 🟡 | `(app)/page.tsx` لوحة عامة، بلا «لم يُردّ عليها» ولا هدف الشهر |
| **قائمة الإسناد** | ❌ | |
| **إسناد مشروع** | ❌ | |
| **محرّك اختيار الفريلانسر** | ❌ | |
| إضافة خطوة | ❌ | |
| التسليم + `qa_issues` | ❌ | |
| لوحة التشغيل | ❌ | |
| **الماليات** (٤ شاشات) | ❌ | |
| إدارة الأدوار من الشاشة | ❌ | `settings/users` يدير المستخدمين فقط |
| إدارة القوائم والإعدادات | ❌ | |
| قائمة الأسعار | ❌ | الجدول موجود بلا شاشة |
| عدّاد التنبيهات المفتوحة | ❌ | |
| سجل التدقيق | ❌ | |
| **التحليلات** (§١٣ كلها) | ❌ | لوحة التحكم الحالية إحصاءات بسيطة لا تحليلات |

**الموجود ومطابق ✅:** الواجهة عربية RTL كاملة · الحفظ موثوق (POST→تحويل) ·
التنقّل موثوق · جلسات آمنة (`auth.ts:39-72`) · كلمات مرور مجزّأة بـbcrypt `:33` ·
حجب المسارات بالجلسة (`src/middleware.ts`) · نشر تلقائي مع فحص آلي.

---

## ٤. المعادلات والمنطق (§٨)

**❌ لا شيء منها موجود.** لا `weighted_pages` ولا معاملات جهد ولا تخصص ولا تكلفة
داخلية ولا خارجية ولا هامش ولا إيراد منسوب ولا استيعاب طاقة ولا ROI ولا أزمنة دورة.

الموجود حسابيًا: جمع بنود عرض السعر وخصم وضريبة في `src/lib/mutations.ts:432-437`
— لا علاقة له بمعادلات المواصفة.

---

## ٥. تعارضات تحتاج قرارك قبل التنفيذ

> المستند يُلزمني بالتوقف والسؤال عند التعارض (§٠).

### ت-١: `Deal` وحدها تخدم دورين متعارضين
القائم يستخدم `Deal` كـ«فرصة بيعية» بمراحل بيعية. المواصفة تريد `projects`
بحالات تشغيلية، **ومرحلة البيع كلها انتقلت إلى `leads`**.

**المقترح:** تحويل `Deal` → `Project` بحالات §٦، ونقل مراحل البيع إلى `Lead`.
لوحة السحب والإفلات الحالية تُعاد استخدامها لحالات المشروع.

### ت-٢: `Company` + `Contact` مقابل `clients` الواحد
**المقترح:** `Client` واحد بحقل `type`، مع الإبقاء على `Company` كجدول اختياري
للشركات (اسم تجاري، رقم ضريبي، شروط دفع) يرتبط به العميل. يمنع ازدواج البيانات
ويحافظ على شاشات الشركات القائمة.

### ت-٣: عمق الترحيل (§١٤)
ترحيل المصادر الثلاثة عمل كبير مستقل (١٠ آلاف صف بعيوب موثّقة). المستند يضعه
**آخر أولوية (§١٨ بند ١٠)** — سألتزم بذلك ولن أبدأه قبل استقرار السكيما.

---

## ٦. نظام نسب المبيعات (طلب إضافي — خارج المستند)

**❌ غير موجود، وغير مذكور في المواصفة.**

المطلوب كما فهمته: نسبة على المبيعات بشرائح دخل لكل أدمن، تذهب للأدمن أو للمدير
أو تُقسَّم بينهما بنسب قابلة للضبط، ويرى البائع ما حققه دوريًا.

**لا أستطيع تنفيذه بلا إجابات** — المستند يمنع اختراع منطق العمل، وكل خيار هنا
يغيّر الأرقام جوهريًا. الأسئلة في §٧ من هذا التقرير ومكرّرة في الخطة.

الجداول المقترحة عند الاعتماد:
`commission_schemes` (الخطة وشرائحها) · `commission_scheme_tiers` ·
`commission_assignments` (من يخضع لأي خطة ومتى) · `commission_entries`
(الاستحقاق المحسوب لكل مشروع لكل مستفيد) — كلها مشتقة، ولا تُدخَل يدويًا.

---

## ٧. أسئلة تحجب التنفيذ

**عن نظام النسب (٦ أسئلة):**
1. **أساس الاحتساب:** نسبة من ماذا؟ إجمالي المبيعات (`net_total`) أم المحصَّل فعلًا أم **الهامش**؟
2. **توقيت الاستحقاق:** عند `سُلّم` أم عند `محصَّل`؟ (المواصفة تعترف بالإيراد عند «سُلّم» — §٣ بند ٤)
3. **طبيعة الشرائح:** تصاعدية جزئية (كل شريحة على الجزء الواقع فيها) أم الشريحة المحققة تُطبَّق على المبلغ كله؟
4. **مثال رقمي واحد** من عندك: بائع حقّق ١٠٠ ألف في الشهر → كم يستحق بالضبط؟ (هذا يحسم ٣ أسئلة دفعة واحدة)
5. **حصة المدير:** إضافية على حصة الأدمن أم مقتطعة منها؟ وهل تختلف بالشريحة؟
6. **الاسترداد:** مشروع أُلغي أو رُدّ بعد احتساب النسبة — تُخصم؟

**عن المواصفة (٣ أسئلة):**
7. **الفروع الستة:** هل يُقيَّد المستخدم بفرعه في الرؤية، أم الفرع للتصنيف والتقارير فقط؟
8. **تسلسل المبيعات:** مديرا مبيعات وفريقان — هل تريد `reports_to` حرًّا (شجرة أي عمق) أم مستويين فقط؟
9. **`Quote`:** أُبقيه أداة مبيعات مربوطة بالمشروع، أم أحذفه اكتفاءً بتسعير المشروع؟

---

## ٨. تقدير الحجم

| المرحلة (§١٨) | الحالة | الحجم |
|---|---|---|
| ١ — البيانات + الصلاحيات كبيانات | ❌ من الصفر | كبير |
| ٢ — العملاء والليدز + البحث بالهاتف | 🟡 تعديل | متوسط |
| ٣ — المشاريع والتحويل والحالات | 🟡 تحويل `Deal` | كبير |
| ٤ — الإسناد والخطوات ومؤشر التكلفة | ❌ | كبير |
| ٥ — التسعير والخصم والاعتماد | 🟡 | متوسط |
| ٦ — الفريلانسرز | ❌ | كبير |
| ٧ — الماليات | ❌ | متوسط |
| ٨ — التحليلات | ❌ | كبير |
| ٩ — التنبيهات | ❌ | متوسط |
| **٩.٥ — نظام النسب** (مُضاف) | ❌ | متوسط |
| ١٠ — الترحيل | ❌ | كبير |

**ما يبقى من القائم ويُبنى عليه:** طبقة المصادقة والجلسات · نمط الحفظ الموثوق ·
الواجهة العربية ومكوّناتها · النشر والفحص الآلي · شاشات الشركات والمهام والملاحظات
وعروض الأسعار (بتعديل) · لوحة السحب والإفلات.

**التقدير الصادق:** المنجَز يمثّل نحو **٢٠٪** مما تطلبه المواصفة — والـ٢٠٪ في
الطبقة التحتية الصحيحة، لا في المنطق المحاسبي الذي هو جوهر المشروع.
