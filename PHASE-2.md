# المرحلة ٢ — العملاء والليدز والبحث بالهاتف ✅

> منجَزة. أول قيمة يلمسها الفريق يوميًا.

## القاعدة التي وُجدت المرحلة من أجلها

> **«لا يُنشأ عميل مكرر أبدًا.»** — §4.1

المنع يقع على **قيد فريد في قاعدة البيانات** على الهاتف بعد تطبيعه، لا على فحص
في الواجهة. الفرق ليس شكليًا: الفحص يسقط حين يحفظ موظفان في اللحظة نفسها،
والقيد لا يسقط أبدًا.

## قاعدة تطبيع الهاتف — سبع خطوات (§١٤)

```
١ احذف كل ما ليس رقمًا
٢ احذف محارف الاتجاه غير المرئية
٣ احذف "00" البادئة
٤ ١١ رقمًا تبدأ بـ01  ← أضف 20
٥ ١٠ أرقام تبدأ بـ1   ← أضف 20
٦ ١٢ رقمًا تبدأ بـ20  ← اتركه
٧ "0" أو أقل من ٨ أرقام ← NO_PHONE
```

النتيجة عمليًا: هذه الصيغ الأربع **تنتج مفتاحًا واحدًا**، فيستحيل تكرار العميل
مهما اختلفت طريقة كتابة رقمه:

```
01001234567  ·  +20 100 123 4567  ·  00201001234567  ·  ⁦201001234567⁩
```

**الخطوة ٢ ليست تفصيلًا.** الأرقام المنسوخة من واتساب تحمل محارف اتجاه غير
مرئية، وبدون حذفها نفقد نحو ١٠٪ من المطابقات — أي عميل من كل عشرة يتكرر.

أُضيفت خطوتان فوق المواصفة، وكلتاهما تخدم القاعدة نفسها:

- **الأرقام العربية** (`٠١٠٠١٢٣٤٥٦٧`) تتحول إلى لاتينية — يكتبها الفريق على الجوال.
- **الكسر العشري يُقطع قبل حذف الرموز.** الرقم المخزَّن عددًا في إكسل يخرج
  `201117741994.0`؛ لو حُذفت النقطة أولًا لالتصق الصفر فصار ثلاث عشرة خانة ولم
  يطابق شيئًا. المواصفة تذكر هذه القيمة بعينها، وقد كسرت التنفيذ الأول فعلًا —
  أمسكها الاختبار قبل أن تصل إليك.

**٢٠ اختبارًا آليًا** لهذه القاعدة وحدها، تعمل بلا متصفح ولا قاعدة بيانات
(`npm run test:phone`)، وتعمل في كل فحص آلي.

## شاشة «ليد جديد» — الهاتف أولًا

1. **حقل الهاتف أول عنصر في الشاشة**، ويبحث في العملاء **أثناء الكتابة**.
2. **إن وُجد العميل:** تظهر بطاقته — الاسم وعدد مشاريعه وإجمالي مشترياته وآخر
   تعامل — وزرّ **«مشروع جديد مباشرة»**. العميل العائد لا يمرّ بمرحلة الليد أصلًا.
3. **إن لم يوجد:** اسم + قناة + وسيلة تواصل. **الفرع والموظف المسؤول يُملآن
   تلقائيًا** من حسابك.

كل ما عدا ذلك داخل «تفاصيل إضافية» مطوية، حتى لا تدفع الحالةُ البسيطة ثمنَ
الحالة النادرة.

> معيار القبول ≤١٥ ثانية. **المقيس: ٠٫٣ ثانية.**

## البحث الفوري — كيف نُفِّذ بلا كسر ما يعمل

البحث ينادي `/api/clients/lookup` بـ`fetch` مباشر، **لا بتنقّل من جهة المتصفح**.
هذا مقصود: مسار التنقّل الداخلي هو ما ثبت تعطّله سابقًا وأجبرنا على نمط
«إرسال ثم تحويل». الـ`fetch` لا يمرّ به.

النداء مؤجَّل ٢٥٠ ملّي ثانية بعد آخر حرف — يكفي ليبدو فوريًا، ويمنع نداءً لكل
ضغطة زر.

> معيار القبول ≤٣٠٠ ملّي ثانية. **المقيس: ٢٢ ملّي ثانية.**

## بطاقة العميل

عدد مشاريعه · إجمالي مشترياته · **متوسط قيمة طلبه** · آخر تعامل · كل مشاريعه ·
كل ليداته · اتصال وواتساب بضغطة.

## الليد — ما أُضيف

| الحقل | لماذا |
|---|---|
| `code` بصيغة `LD-2608-0001` | معرّف يُقال في الهاتف، والعدّاد يتجدّد شهريًا |
| `channel` **إلزامي** | بلا مصدر لا نعرف ما يعمل وما لا يعمل |
| `contactMethod` · `branch` · `estPages` | الفرع يُملأ تلقائيًا من حساب الموظف |
| **`firstReplyAt`** | يُبصم عند **أول** انتقال عن «جديد»، ولا يُعاد ضبطه أبدًا |
| `closedAt` | وقت الحسم فوزًا أو خسارة |
| **`lossReason` إلزامي عند الخسارة** | **يمنع الحفظ بدونه** |

### المراحل الست (§4.2)

```
جديد ← تم التواصل ← عرض سعر ← تفاوض ← فائز / خاسر
```

بديلًا عن الخمس القديمة. زر «خاسر» **لا يعمل بضغطة** — يمرّ بنموذج التعديل لأن
السبب إلزامي. هذا احتكاك مقصود: الخسارة بلا سبب بيانة ضائعة.

## «لم يُردّ عليها بعد»

شريط أحمر أعلى قائمة الليدز يعدّ ما تجاوز مهلة أول رد (`sla_first_reply_hours`،
٣ ساعات افتراضيًا) وما زال «جديدًا». الصفوف المتأخرة تُظلَّل. المهلة تُعدَّل من
شاشة الإعدادات.

## الاختبارات

**٢٦ سيناريو في متصفح حقيقي + ٢٠ اختبار وحدة للتطبيع.** الجديد منها:

| # | ما يتحقّق منه | النتيجة |
|---|---|---|
| ٢٠ | عميل عائد بنفس الهاتف بصيغة مختلفة لا يُنشئ عميلًا ثانيًا | ١ قبل و١ بعد |
| ٢١ | سرعة البحث بالهاتف | ٢٢ ملّي ثانية |
| — | ليد جديد ≤١٥ ثانية | ٠٫٣ ثانية |
| — | لا يُحفظ ليد خاسر بلا سبب | يُرفض |
| — | البحث الفوري يتعرّف على العميل رغم اختلاف صيغة الرقم | يتعرّف |

## أثر النشر على قاعدة البيانات القائمة

هذه المرة **لا شيء يضيع ولا شيء يحتاج تدخلًا يدويًا.** التجهيز يفعل تلقائيًا:

1. **يرحّل المراحل القديمة:** `مؤهَّل ← تفاوض` · `غير مؤهَّل ← خاسر` ·
   `تم تحويله ← فائز`. والخاسر بلا سبب يُعلَّم بـ«أخرى» صراحةً بدل تخمين سبب.
2. **يُنشئ عميلًا لكل ليد بلا عميل**، بمطابقة الهاتف المطبَّع — فليدان لنفس
   الرقم بصيغتين مختلفتين يلتقيان على **عميل واحد**.
3. **يملأ المعرّفات التسلسلية**، مرقِّمًا كل ليد **بشهر إنشائه هو** لا بشهر
   التشغيل، حتى تبقى الأرقام ذات معنى.

جُرِّب فعليًا على قاعدة تحمل صفوفًا بالصيغة القديمة قبل الرفع.

## ما بعدها

المرحلة ٣: المشاريع والتحويل والحالات الثماني — الكائن المحوري الذي تدور حوله
كل الحسابات. راجع `PLAN.md`.
