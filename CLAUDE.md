# ذاكرة المشروع — Fast Trans CRM

> **هذا الملف يُقرأ في كل جلسة قبل أي عمل.** فيه ما لا يجوز نسيانه: القواعد
> الملزمة، والقرارات المحسومة، والأخطاء التي وقعت مرة ولا تتكرّر.
>
> **ونحدّثه معًا:** كل قرار جديد، وكل قاعدة عمل تُقرّها الإدارة، وكل خطأ
> يُكتشف — يُضاف هنا في حينه، لا في نهاية المرحلة.
>
> آخر تحديث: المرحلة ١٦ · الكوميت `5a3bc62`+

---

## ١ · ما هو هذا النظام

نظام تشغيل متكامل لمكتب **فاست ترانس** للترجمة المعتمدة — لا CRM عام.

ستة فروع (المقطم · المهندسين · الإسكندرية · مدينة نصر · الرياض · بريدة)،
~١٥ موظفًا داخليًا، و١٠٦٧ مترجمًا خارجيًا. العملة الأساسية الجنيه المصري،
وفرع الرياض يبيع بالريال.

**خطّا عمل في نظام واحد:**
- **(أ) ترجمة معتمدة لمستندات شخصية** — أغلب الحجم، طلبات صغيرة متكررة،
  وسيط الطلب ~٥٠٠ ج. عمل «كاونتر» لا مشروع.
- **(ب) ترجمة متخصصة وتوطين للشركات** — قانوني وطبي ومالي وتقني.

**والقاعدة: نفس الشاشات تخدم الاثنين، بلا أن تُبطئ متطلباتُ (ب) تشغيلَ (أ).**

المالك: **أحمد محسن** — غير مبرمج. الشرح يكون بلغة العمل لا بلغة الكود.

---

## ٢ · المستندات الملزمة

| الملف | ما هو | حجّيته |
|---|---|---|
| `docs/SPEC.md` | المواصفة العربية (٧١٣ سطرًا) المشتقّة من تدقيق بيانات فعلية | **ملزمة** |
| `docs/FINANCE-SPEC.md` | مواصفة الماليات من دفاتر المكتب الفعلية | **ملزمة** للدفاتر |
| `docs/BRANCH-SPEC.md` | محاسبة الفروع وتوزيع التكلفة المركزية | **ملزمة** للفروع |
| `COMMISSION-SPEC.md` | أمثلة النسب التي أقرّتها الإدارة بأرقامها | **ملزمة** |
| `docs/HANDOVER.md` | مستند التسليم الهندسي الكامل (١٧ قسمًا، بالإنجليزية) | مرجع |
| `docs/ARCHITECTURE.md` | مراجعة الباك إند وتنظيم البيانات (بالعربية) | مرجع |
| `FIELD-MAPPING.md` | مطابقة أسماء المواصفة بأسماء الكود | مرجع |
| `PHASE-1..16.md` | سجل كل مرحلة: ما بُني وما تقرّر وما أُجّل | تاريخي |

---

## ٣ · القواعد الملزمة — لا تُخالَف

1. **افحص الصلاحية لا اسم الدور.**
   `can(user, 'canViewSellPrice')` ✅ · `user.roleName === 'sales_admin'` ❌
   الأدوار بيانات تُنشأ من الشاشة بلا نشر. أي شرط على اسم دور يتعطّل فور
   إنشاء الإدارة دورًا جديدًا.

2. **الترشيح في الخادم.** الحقل الذي لا يملك المستخدم صلاحيته **لا يُرسل
   أصلًا** — لا يُخفى بـCSS ولا يُرشَّح في المتصفح. (§٣ بند ٢، وله اختبار
   e2e يفتّش حمولة الاستجابة عن `costInternal` و`marginPct` و`staffSalary`.)

3. **لا شيء يُمحى.** حذف منطقي + سجلّ تدقيق دائم. القيد الخاطئ **يُعكَس
   بقيد مضادّ** ولا يُعدَّل. النسبة المستردّة قيد عكسي سالب لا حذف.

4. **الاعتراف بالإيراد بالحالة.** لا إيراد ولا تكلفة في أي تقرير مالي إلا
   لمشروع `delivered` أو `collected`. الإلغاء يمسح `revenueMonth`.

5. **لا تخترع منطق عمل.** كل معادلة في المواصفة مقصودة. إن سكتت المواصفة أو
   تعارضت — **توقّف واسأل أحمد**، ولا تجتهد.

6. **العربية RTL هي الواجهة الوحيدة**، والمفاتيح البرمجية إنجليزية،
   والتعليقات عربية تشرح **لماذا** لا ماذا.

7. **العمود الجديد يُولد قابلًا للفراغ** (أو بقيمة افتراضية)، فـ`db push`
   على قاعدة الإنتاج المملوءة **يضيف ولا يحذف**.

8. **القاعدة الجديدة تدخل ومعها اختبارها** في نفس الكوميت.

9. **لا يُكتب معرّف النموذج** (`claude-opus-*`) في كوميت أو تعليق أو أي أثر
   في المستودع — في المحادثة فقط.

10. **الفرع:** `claude/crm-sales-team-system-1f54m8` — والدفع
    `git push -u origin claude/crm-sales-team-system-1f54m8`.
    **ولا يُفتح Pull Request إلا بطلب صريح.**

---

## ٤ · القرارات المحسومة — لا تُراجَع بلا سبب جديد

| القرار | السبب المقيس |
|---|---|
| **لا Server Actions** — نماذج POST تقليدية ثم تحويل 303 | كانت تتعطّل ~٥٠٪ من المرات: يُحفظ السجل ولا تنتقل الواجهة |
| **لا `next/link`** — وسم `<a>` عادي في `src/components/link.tsx` | التنقّل كان يفشل ~مرة من كل ثلاث |
| **الفصل بين القواعد الخالصة والمحرّكات** | ٩٦٥ اختبارًا تعمل في ثانية بلا قاعدة بيانات |
| **التكلفة تُجمَّد عند التسليم** | تغيّر راتب الشهر القادم لا يجوز أن يُعيد كتابة هوامش الشهر الماضي |
| **الأسعار بتاريخ سريان** | تعديل سعر اليوم لا يغيّر فاتورة الماضي |
| **الدفتر يستقبل مسوّدات** — والمحاسب وحده يرحّل | الدفتر لا يُكتب من ظهره |
| **الذكاء الاصطناعي بلا صلاحيات خاصة** — سياقه بصلاحيات سائله | وإلا صار بابًا خلفيًا لكل ما حُجب في §٥ |
| **لا يُرسَل ردّ بريد تلقائيًّا** | الصادر باسم المكتب التزامٌ عليه، ولا يُلزمه ما لم يقرأه إنسان |
| **قبول العرض الاحترافي لا يفتح مشروعًا** | العرض إطار سعري لشهور، والمشروع يُفتح بدفعة فعلية |
| **`/api/cron` مغلق افتراضيًّا** بلا `CRON_SECRET` | مسار يفتح صناديق البريد بلا حماية بابٌ مفتوح |
| **المنفِّذ الداخلي سجلٌّ بلا دخول** — بريده وكلمة مروره اختياريان | المترجم يُسنَد إليه وتُحسب تكلفته بلا حساب يفتحه هو |
| **الملفات تُخزَّن في القاعدة لا على القرص** | النشر بلا قرص دائم: ملفٌ يُكتب اليوم يختفي مع أول إعادة نشر |
| **HTML المولَّد يمرّ بثلاثة حواجز**: تعقيم عند الحفظ وعند العرض + سياسة محتوى وإطار معزول | التعقيم بالأنماط وحده رهانٌ لا يُبنى عليه |
| **الأجر بنودٌ بتاريخ سريان** — والبند لا يُعدَّل بل يُنهى ويُنشأ خلفه | تعديل بندٍ سارٍ يُعيد كتابة كشوف الماضي بأثر رجعي |
| **معدل التحميل مثبَّت لسنة** لا محسوبًا شهريًا | الشهري يُلغي التخطيط ويعاقب الفرع على أداء غيره ويُخفي الرفع التشغيلي وتكلفة الطاقة العاطلة |
| **فرق الاسترداد لا يُوزَّع** — يبقى بندًا في المركز | توزيعه يُخفي تكلفة الطاقة غير المستغلة، وهي أهم مؤشر لقرار التوسّع |
| **قرار الإغلاق على هامش المساهمة حصرًا** | المركز ثابت لا يزول بالإغلاق: إغلاق فرعٍ مساهمته موجبة يبدأ دوامةً تنتهي بانهيار الشركة |
| **`is_cash` على الحساب لا على السطر** | حقلٌ يُملأ يدويًا آلاف المرات يُملأ خطأً مرة على الأقل |
| **`canManageRoles` منفصلة عن `canManageUsers`** | مدير المبيعات يضيف أدمنز؛ ولو كانتا واحدة لأعاد معها تعريف من يرى ماذا |
| **لا يمنح أحدٌ ما لا يملك** — الدور المُسنَد لا يحمل صلاحيةً يفتقدها المُسنِد | فتحُ «إدارة المستخدمين» لمدير المبيعات بابٌ للتصعيد بلا هذا الحارس |
| **عتبة الاستحقاق على الإسناد لا على الخطة** | يشترك عشرون في خطة واحدة ولكلٍّ تارجته المحسوب له |
| **ما دون العتبة لا شيء البتّة** — لا نسبةً مخفَّضة | النسبة مكافأةُ تجاوزٍ لا أجرُ حضور، والتخفيض يُلغي معنى العتبة |
| **مالكٌ واحد للصفقة: من أدخلها** — والملكية المزدوجة أُلغيت (المرحلة ١٦) | «حتى لا نتلخبط»: الأدمن ٣٪ ومديره ٢٪، ولا بُعد ثانيًا يقسم الحصص |
| **الصفحة هي الوحدة والإجمالي مشتقّ** — لا خانة «إجمالي المشروع» | مصدران للرقم الواحد يختلفان، ولا يُعرف أيّهما الصحيح حين يختلفان |
| **شرحُ ما كان معطلًا لا يُكتب في الشاشة** — مكانه `PHASE-N.md` | المستخدم لم يرَ الخلل السابق، وشرحُه له تشويشٌ في شاشةٍ يفتحها كل يوم |
| **تغييرُ القاعدة ملفُّ SQL يُقرأ قبل أن يُطبَّق** — لا دفعًا أعمى | `db push --accept-data-loss` يُطابق القاعدة بالمخطط مهما كلّف، وأمانُه عُرفٌ كُسِر مرة فأسقط النسخة العاملة |
| **ملفات الترحيل بلهجة PostgreSQL وحدها** — وجهاز التطوير يبقى على `db push` | قاعدةُ التطوير تُعاد في ثانية فلا تحتاج تاريخًا؛ وملفٌ بلهجتين لا يعمل بواحدة منهما |
| **العمود لا يُحذف بل يُتقاعد** — ويحرسه `check:additive` | النشر يدفع القاعدة **قبل** أن يبني الكود، فحذفُ عمود يُسقط النسخة العاملة قبل وصول بديلها |
| **إخفاء الرابط من القائمة ليس حجبًا** — الحجب في الصفحة | من يعرف المسار يكتبه، والقائمة زينةٌ لا حارس |
| **صفُّ القيد يُغني عن فتحه** — الحسابان والعميل والأدمن والفرع فيه | المحاسب يراجع عشرات القيود في الجلسة، وفتحُ كلٍّ منها رحلةٌ ذهابًا وإيابًا |
| **الرسوم HTML خالص بلا مكتبة** — ومع كل شريط رقمُه | تعمل في الخادم وتُطبع ويقرأها قارئ الشاشة، والمعنى لا يعتمد على اللون |
| **الموارد البشرية تُعيّن، وإدارة المستخدمين تفتح الحساب** | من يفتح الحساب هو من يملك منح الصلاحيات، لا من يملك ملف الموظف |
| **دور «مترجم» بلا صلاحية واحدة** | شاشة «أدائي» لا تحتاج إذنًا بل صاحبها — وكل صلاحية تُمنح له تفتح ما لا يخصّه |
| **دفتر المحاسب يُجمَّع بالرصيد الجاري** لا برقم قيد | ورقته مسطَّحة بلا أرقام قيود؛ والقيد صفوفٌ متتالية يتوازن عندها المجموع |
| **ما لا يتوازن لا يدخل الدفتر ولا يُجبَر** | إجبارُ الاتزان يُخفي خطأً في ورقة المحاسب بدل أن يكشفه |
| **المصروف التسويقي مُدخَلٌ مرة واحدة: في الدفتر** — والتحليلات تقرؤه ولا تسأل عنه | شجرة الحسابات فيها «مصاريف بيعية وتسويقية» بفروعها؛ وخانةُ إدخالٍ ثانية تُنتج رقمين لشيء واحد لا يُعرف أيّهما الصحيح |
| **المهمة المجدولة نداءٌ مستقلّ بجدوله — ولها بصمةُ نجاحٍ تُقرأ** | المهمة التي تموت في مهلة الدالة تموت صامتة: لا رسالة خطأ، فقط تنبيهاتٌ تكفّ عن الوصول شهورًا ولا يعلم أحد |
| **المال يُجمع بالقرش الصحيح لا بـ`+` على الجنيهات** — `sum` لا `reduce` | الفاصلة العائمة لا تعرف عُشرًا؛ والكسر ينحرف عبر آلاف القيود حتى يُخرج ميزان المراجعة عن التوازن |
| **نسبة الخصم مئويّة في كل الشاشات** | شاشتان بوحدتين جعلت من يكتب `10` يمنح ألفًا بالمئة |
| **النسخة التي لا تملكها ليست نسخةً احتياطية** | مزوّد القاعدة يحتفظ بنسخه؛ وحسابٌ يُغلق أو خطأٌ في لوحته يُذهب سجلَّ العملاء والدفاتر بلا رجعة |
| **سجلُّ الأعطال يُنقّى قبل أن يُكتب** — ولا يُصدَّر سرٌّ في نسخة | رسالةُ خطأٍ من القاعدة تحمل رابط الاتصال بكلمة مروره، وشاشةٌ يفتحها المالك ليست مكانًا له |
| **الجودة كثافةُ ملاحظاتٍ لكل ألف كلمة** لا عددًا مجرّدًا — والوحدة واحدة في كل شاشة | المعيار الصناعي (SAE J2450 · LISA QA · MQM) ينسب الخطأ إلى حجمه؛ والعدد المجرّد يعاقب من ترجم خمسين صفحة ويكافئ من ترجم خمسًا |
| **مؤشرا جودةٍ لا واحد: كثافةُ الملاحظات ونسبةُ الإعادة** | الأولى أخطاءٌ أمسكها المكتب قبل العميل، والثانية أخطاءٌ أفلتت بعده — وصفرُ ملاحظاتٍ مع إعادةٍ متكرّرة ليس جودةً بل مراجعةً لا تعمل |
| **قسمُ التحليلات يتبع عملَ صاحبه** — البيعُ لمن يبيع والإنتاجُ لمن يُسند | كان أداءُ المنفِّذين محجوبًا خلف «تحليلات الشركة» وأداءُ البائعين مفتوحًا لكل داخل — فيرى مديرُ المشاريع بيعًا لا قرار له فيه، ولا يرى إنتاجيةَ فريقه وهي عملُه كلّه |
| **`collectNow` دفعةُ اليوم و`collectedAmount` الرصيدُ كلّه** — والجمع في الخادم | لو حسب المتصفحُ المجموعَ وأرسله، لكفى أن يفتح أدمنان الصفَّ نفسه في لحظتين متقاربتين حتى تُكتب دفعةٌ فوق أختها فيضيع مبلغٌ من الدفتر |
| **من يُسند ولا يبيع له لوحته وأرقامه** — والشرط صلاحيةٌ لا اسمُ دور | مدير المشاريع كان يفتح لوحةً مبنيّةً لأدمن المبيعات: مسار مبيعات وترتيب بائعين — أرقامٌ لا قرار له فيها ولا يصحّ أن يراها |
| **الوصول إلى المشاريع من `canAssignProduction` لا من `canViewAllLeads`** | الصلاحية الواسعة ثمنٌ باهظ لحاجةٍ ضيّقة: مُنحت ليصل إلى قائمة الإسناد فحملت معها جردَ الليدز والعملاء وأرقام الفرق |
| **الراتب مصدره الموارد البشرية وحدها** — و`StaffCost` للنسبة والطاقة | رقمان لشيء واحد يختلفان: راتبٌ في ملفّ الموظف وآخر في شاشة التكاليف، فيقول النظام «منتِج بلا تكلفة» ويحسب هامشًا ١٠٠٪ وهميًّا |
| **لوحة الترتيب لوحةُ فريقٍ لا لوحةُ شركة** | مديرُ المبيعات يرى أرقام فريقٍ آخر فتختلط المنافسة؛ ولا يراها كاملةً إلا المالك والمدير التنفيذي والمحاسب |
| **العميل يتبع من باع له لا من كتب بطاقته** | البيانات تُستورد دفعةً واحدة فتحمل البطاقات كلها اسم المستورد؛ والترشيح بمُنشئ السجل يُري مديرَ المبيعات صفرًا وفريقُه قد باع لسبعين |
| **الفلترة بالموظف حقُّ كل من يرى أكثر من واحد** لا صاحب رؤية الشركة وحده | «ماذا فعل هذا الأدمن بالذات؟» أولُ أسئلة مدير الفريق، وكان الشرط `seeAll` يحرمه منه |
| **الزرّ الذي يُبدّل صفًّا يطلب `json=1` من نفس بوابة `/api/save`** — والنموذج تحته يبقى POST عاديًا | ترحيلُ ثلاثين قيدًا كان يُعيد بناء الصفحة ثلاثين مرة؛ وتغييرُ **شكل الردّ** لا **مساره** يُبقي القواعد والصلاحية والتدقيق في مكان واحد، ويُبقي الشاشة عاملةً بلا جافاسكربت |

**النتيجة الجانبية المقصودة: النظام يعمل كاملًا ولو تعطّل الجافاسكربت.**

---

## ٥ · خريطة الكود السريعة

```
src/lib/*.ts            قواعد خالصة — بلا قاعدة بيانات ولا شبكة ★ كل منطق العمل هنا
src/lib/*-engine.ts     محرّكات server-only — تلمس القاعدة والشبكة
src/lib/ledger.ts       دفتر الأستاذ (server-only)
src/lib/analytics.ts    التحليلات (server-only)
src/lib/mutations/      ١٨ وحدة بالمجال + base.ts + index.ts (الواجهة)
src/app/api/save        بوابة الحفظ الموحّدة — ٦٩ كيانًا
src/app/api/mutate      تعديلات سريعة — ١٨ عملية
src/app/(app)/**        ٩٦ صفحة — عرض فقط، لا حساب
tests/*.mjs             ١٬١٠٦ اختبار وحدة + ٢٣٦ e2e
```

**المعيار: إن لم تستطع اختبار الكود بلا قاعدة بيانات، فهو في الطبقة الخطأ.**

**الميزة الجديدة = ملف جديد في `mutations/`**، لا إضافة إلى ملف مجال آخر.
والاستيراد دائمًا من `@/lib/mutations` لا من الملف مباشرةً.

---

## ٦ · أرقام النظام

| | |
|---|---|
| جداول · فهارس | ٥٦ · ٩٧ |
| صفحات · مسارات API | ١٠٥ · ١١ |
| صلاحيات · أدوار افتراضية | ٢٦ عمودًا منطقيًا على `Role` · ١٠ أدوار · ٩ شاغلين |
| اختبارات | ١٬٢٩٣ وحدة + ٢٨٢ e2e |
| بيانات الإنتاج | ٣٬٤٤٨ عميلًا · ٢٬١٥٤ ليدًا · ٣٬١٠٥ مشروعًا · ٣٬١١٥٬٠٤١ ج |
| المراحل المنجزة | ١٦ |

**الهوية — أربعة ألوان مقروءة من ملفات الشعار الرسمية لا مقدَّرة بالعين:**
الكحلي `#242E5B` (`brand-600`) · الأزرق `#2B57A6` · السماوي `#5BB5E8`
(`marine-*`) · الليموني `#C4D82E` (`lime-400`).
> الليموني كان `#B9D716` مقدَّرًا من عرض تقديمي — صُحِّح في المرحلة ١٤.
> والعلامة **رأسية بنسبة ١٢٠:٢٠٦**، فلا تُرسم في مربّع.

---

## ٧ · أخطاء وقعت مرة — لا تتكرّر

| الخطأ | السبب الجذري | الحارس الآن |
|---|---|---|
| خصم النسبة مرتين عند الإلغاء | `rebuildPeriod` أبقى قيودًا عكسية يتيمة | تُمسح قبل إعادة الحساب |
| ٤٦ بطاقة تنبيه لحدثين | الأحداث الفورية كانت بمفتاح لكل سجل | بطاقة واحدة لكل شخص لكل حدث، تُحدَّث مكانها |
| شاشة الإسناد غير صالحة | قائمة منفِّذين فارغة، ولا فريلانسر ولا مدير مشاريع | `anyOf` في قاعدة الانتقال + تسمية الدور بنمط التشغيل |
| تواريخ الترحيل لا تُصلَح أبدًا | رفض المستقبل كان **قبل** إصلاح انقلاب مارس | المحلّل يعيد `future` والمستورد يحكم بعد الإصلاح |
| سعران بنفس اليوم بلا فاصل | الترتيب بـ`effectiveFrom` وحده | فاصل `createdAt desc` |
| سلاسل الاقتباس العربية تدخل التحليل | النمط توقّع `كتب:` مباشرةً | يسمح باسم بين الفعل والنقطتين |
| التوقيع العربي لا يُنزع | `\b` في جافاسكربت **لاتينيّ صرف** فلا يقع بعد حرف عربي | حُذف `\b` من أنماط التوقيع |
| حفظ العرض الاحترافي ينكسر | حقل `entity` اصطدم بحقل التوجيه في `/api/save` | صار `contractingEntity` |
| الحفظ يتجمّد في التطوير | قفل SQLite أحادي الكاتب خلف قراءة الصفحة | WAL + `busy_timeout` + `synchronous=NORMAL` |
| نمط تنقية أسماء الملفات كُتب ببايتات تحكّم حرفية | كتابة الملف من heredoc بلا هروب | صيغة `\u0000-\u001f` المهروبة، وفحصٌ يمنع عودتها |
| العلامة تُرسم مشوَّهة في الترويسة | كتلة الشعار **رأسية ١٢٠:٢٠٦** ورُسمت في مربّع `h-8 w-8` | العرض يُشتقّ من الارتفاع في كل موضع |
| «المدير التنفيذي» غائب عن «يتبع إداريًا» | الحقل سليم — **الدور لم يكن موجودًا فلم يشغله أحد** | الدور في `DEFAULT_ROLES`، والتسلسل يبقى بياناتٍ في `reportsToId` |
| خانة نسبة الخصم تقبل ألفًا بالمئة | شاشة المشروع تطلبها **عشريّة** وشاشة العرض **مئويّة** | `readDiscountFields` — مئويّة في الشاشتين، والتحويل عند حدّ النموذج |
| ميزان المراجعة يختلّ بقرش بعد الترحيل | تراكم تقريب ٢٬٢٤٧ قيدًا | التسوية على آخر سطر عند كتابة كل قيد |
| كل شريط في «مسار المبيعات» يفتح ٤٠٤ | رابط `‎/deals?stage=` بقيّةٌ من التسمية قبل أن تصير الصفقة مشروعًا | `‎/projects?status=` — وفُتّش المستودع فلم يكن غيرَه |
| شاشة الإسناد تسأل عن مراجعٍ مرتين | اسم دور المنفِّذ في نمط الآلة كان «المراجع البشري بعد الآلة» | صار «المنفِّذ على الذكاء الاصطناعي»، والشاشة خطوتان مرقَّمتان |
| جدول التحليلات يتكدّس سطورًا على الجوال | عشرة أعمدة في `min-w-[640px]` تُعصر ولا تُمرَّر | `tbl-wide` بعرضه الحقيقي، والأشرطة تسبق الجدول |
| بطاقة العميل تعرض «إجمالي مشترياته» لأي داخل | الشاشة بلا فحص `canViewSellPrice` | الحقل لا يُقرأ من القاعدة أصلًا لمن لا يملكه |
| الفريلانسرز وعروض الأسعار مفتوحة للجميع | الحارس في القائمة الجانبية لا في الصفحة | `requireAnyPermission` في الصفحة نفسها وفروعها |
| مدير المبيعات يفتح سجل العملاء فلا يجد أحدًا | الترشيح على `Client.ownerId` — وهو **من كتب البطاقة** لا من أتمّ البيع، والبيانات استُوردت بحساب واحد | `teamClients`: النطاق اتحادُ من له مشروعٌ يملكه الفريق ومن أنشأ الفريق بطاقته |
| «المنتِج بلا تكلفة مسجَّلة» مع أن راتبه مُدخَل | التكلفة تُقرأ من `StaffCost` والراتب أُدخل في الموارد البشرية — مخزنان لنفس الرقم | `staffRateOf` يشتقّ الأجر من بنود الأجر السارية |
| تنبيه التسليم لا يصل أدمن المبيعات | الحدث «فوريّ» في الكتالوج، لكن الفوريّ لا يُطلق إلا حين يعمل المجدوِل — و`/api/cron` مغلق بلا `CRON_SECRET` | `deliverProject` يُطلق الحدث بنفسه لحظةَ التسليم |
| «سُلّم» صفرٌ في اللوحة والمشروع موجود في «المشاريع» | نطاق اللوحة كان `ownerId: user.id` ونطاق «المشاريع» نطاقَ فريق | اللوحة تستعمل `ownerFilter` نفسه |
| اختبار منتقي الفريلانسر ينهار بعد عشرات التشغيلات | كان يبحث بالبادئة المشتركة «مترجم اختبار»، وكل تشغيل يُنشئ اسمًا بها فيسقط الجديد خارج ما تعرضه القائمة | يُبحث بالجزء الفريد من الاسم لا ببادئته |
| اختبار e2e ينهار بعد بناءٍ جديد | خادمٌ قديم ما زال يمسك المنفذ ٣٠٠٠ فيخدم أجزاءً قديمة | يُقتل بمعرّفه قبل كل تشغيل — لا بنمط يطابق الصدفة نفسها |

**مصائد تسمية:** لا تسمِّ حقل نموذج `entity` ولا `id` ولا `back` — كلها
تصطدم بموجّه `/api/save`.

**مصيدة الشرائح:** شرائح **النسب** تصاعدية على الأجزاء، وشرائح **العروض
الاحترافية** بأثر رجعي على الحجم كله. ٢٠٠ ألف كلمة = ١٤٤٠٠ ريال لا ١٥٢٠٠.

**مصيدة النِّسَب في الأجر:** نسبة البدل تُحسب على **الأساسي** لا على الإجمالي —
وإلا زاد البدل بزيادة بدلٍ آخر فتغيّر الراتب بترتيب الإدخال.

**مصيدة التعادل:** التعادل **قسمةٌ** على (١ − النسب) لا **ضربُ** المصاريف في
(١ + النسبة). والخطأ يُنتج تعادلًا أقلّ من الحقيقي دائمًا: ٦٠٬٠٠٠ بنسب ٤٥٪
و٥٪ = **١٢٠٬٠٠٠** لا ٩٠٬٠٠٠.

**مصيدة القرش الأخير:** كسورٌ أطول من قرشين في ورقة المحاسب تتراكم عبر ألفَي
قيد فتُخرج **ميزان المراجعة** عن التوازن. التسوية على آخر سطر — كما في توزيع
النسب على المشاريع.

**مصيدة الاختبار الذي يكتب الرقم ثم يفحصه:** اختبارٌ يكتب «٥٠٠٠» في خانة
الإجمالي ثم يتحقّق من ٥٠٠٠ **لا يفحص شيئًا** — يتجاوز التسعير كلَّه. يقرأ ما
حسبه النظام.

**مصيدة العتبة:** بلوغُ العتبة استحقاقٌ ولا يُشترط تجاوزها (`>=` لا `>`)،
وعتبةُ صفر تُقرأ «بلا عتبة» لا «عتبةٌ يستحيل عدم بلوغها».

**مصيدة الترتيب:** المتساويان يشتركان في المركز ثم يقفز التالي. الفصل بينهما
بترتيب الحروف أو الإدخال ظلمٌ يراه الموظف في شاشةٍ غرضُها التحفيز.

**مصيدة الصفر:** «غير مقيس» تُعرض `—` لا صفرًا. الصفر يعني «أدّى ولم يُنجز»،
والغياب يعني «لم يُقَس» — والفرق قرارٌ يُتّخذ في حق إنسان.

---

## ٨ · دورة التحقق قبل أي كوميت

```bash
npx tsc --noEmit        # صامت
npm run test:unit       # ١٬٢٩٣
npm run build           # ينجح
npm run start &         # ثم في نافذة أخرى:
CHROMIUM_PATH=/opt/pw-browsers/chromium-1194/chrome-linux/chrome \
TEAM_INITIAL_PASSWORD='FastTrans2026!' npm run test:e2e   # ٢٨٢
```

**بيانات التطوير المحلي:** `ADMIN_PASSWORD='ChangeMe123!'` ·
`TEAM_INITIAL_PASSWORD='FastTrans2026!'` · `admin@fasttrans.local`

**بعد أي صلاحية جديدة:** `npm run bootstrap` لتصل الأدوار النظامية.

---

## ٩ · المخاطر المفتوحة

| # | الخطر | الحال |
|---|---|---|
| ١ | **الأموال `Float` في القاعدة** — والحساب صار بالقرش الصحيح | **مخفَّف جوهريًّا** ✅ — `money.ts` يجمع بالقرش، و`check:money` يفحص الدفتر كلّه. وتحويل نوع العمود إلى `Decimal` يبقى خطوةً لاحقة |
| ٢ | ~~`db push --accept-data-loss` في بناء الإنتاج~~ | **مغلق** ✅ — ملفات ترحيل مراجَعة، والتهيئة تلقائية، وحارسان: `check:additive` و`check:migrations` |
| ٣ | ~~لا تحديد لمحاولات الدخول~~ | **مغلق** ✅ — عشر محاولات في ربع ساعة، والعدّ في القاعدة |
| ٤ | ~~`/api/cron` يعمل داخل الطلب~~ | **مغلق** ✅ — كل مهمة نداءٌ بجدوله في `vercel.json`، وميزانيةُ وقتٍ تقول ما لم يُنفَّذ، وشاشةُ «المهام المجدولة» تكشف الصمت |
| ٥ | ~~لا تجميع أخطاء~~ | **مغلق** ✅ — `ErrorLog` وشاشة «أعطال النظام»، والرسائل تُنقّى قبل حفظها |

**مغلق:** كلمة مرور قاعدة الإنتاج التي ظهرت في لقطة شاشة — **بُدّلت** ✅

---

## ١٠ · متغيّرات البيئة

| المتغيّر | لازم؟ | بلا ضبطه |
|---|---|---|
| `DATABASE_URL` · `AUTH_SECRET` · `ADMIN_PASSWORD` | نعم | لا يعمل شيء |
| `SECRET_KEY` | للبريد | يُشتقّ من `AUTH_SECRET`؛ ويُرفض حفظ الصناديق دونهما |
| `ANTHROPIC_API_KEY` | للذكاء | الأزرار الذكية تختفي والباقي يعمل كاملًا |
| `AI_MODEL` · `AI_DAILY_BUDGET_USD` | لا | نموذج متوازن وسقف ٥$ يوميًّا |
| `CRON_SECRET` | للجدولة | `/api/cron` مغلق (٥٠٣) |

---

## ١١ · أسلوب العمل مع أحمد

- **غير مبرمج** — الشرح بلغة العمل: ماذا يعني هذا لتشغيل المكتب، لا ما هي
  الدالة التي تغيّرت.
- **يرسل ملفات فعلية** (شيتات جوجل، ملفات PDF لعروض حقيقية) — تُقرأ كاملة
  وتُستخرج منها القاعدة، لا يُقدَّر بالعين.
- **يطلب المرحلة كاملة** — تُسلَّم كاملة ومعها مستند `PHASE-N.md`.
- **الكوميت بالعربية**، عنوانه ما تغيّر على مستوى العمل، ومتنه **لماذا**.
- **ما يُترك مفتوحًا عمدًا يُقال صراحةً** ولا يُخفى.
- **لا أسهم في خطوات موجَّهة له** (`←` و`→`): تنقلب في العربية فتصير عكس
  المقصود. الخطوة رقمٌ في سطر، والكلمة الإنجليزية وحدها في آخر الجملة.

---

## ١٢ · متى يُحدَّث هذا الملف

**فورًا** عند:
- قرار معماري جديد → §٤
- قاعدة عمل جديدة تُقرّها الإدارة → §٣ أو الملف المعني
- خطأ اكتُشف وأُصلح → §٧ (السبب الجذري والحارس، لا الوصف)
- خطر جديد أو إغلاق خطر قائم → §٩
- متغيّر بيئة جديد → §١٠
- انتهاء مرحلة → §٦ (الأرقام) + سطر في §٢

**ولا يُحدَّث** بتفاصيل تنفيذية تتغيّر كل أسبوع — تلك مكانها `docs/HANDOVER.md`.
