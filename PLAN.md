# خطة التنفيذ المرحلية

> مبنية على `GAP-ANALYSIS.md` وملتزمة بترتيب الأولويات في §١٨ من المواصفة.
> **معروضة للاعتماد قبل التنفيذ** (§٠ بند ٣).

## قواعد سارية على كل مرحلة

- كل مرحلة تُسلَّم في **Pull Request منفصل** مع اختباراتها، وتُنشر تلقائيًا بعد الدمج.
- **لا مرحلة تُغلق بلا اختبارات آلية** من §١٧ تخصّها.
- معايير السرعة في §١٥ **شرط قبول**: أي شاشة تتجاوزها تُعاد قبل الانتقال.
- الحقول الحساسة **لا تُرسل من الخادم** لمن لا يملك صلاحيتها — يُختبر على JSON لا على الشاشة.
- النظام يبقى **صالحًا للعمل بعد كل مرحلة** — لا مرحلة تكسر ما قبلها.

---

## المرحلة ١ — الأساس: البيانات والصلاحيات كبيانات

**لماذا أولًا:** كل ما بعدها يعتمد عليها، وتغييرها لاحقًا يعني إعادة كتابة.

- جدول `Role` بـ**١٨ عمودًا منطقيًا** + شاشة إنشاء وتعديل الأدوار
- استبدال كل فحص دور في الكود بفحص صلاحية: `can(user,'can_view_sell_price')`
  — يشمل إعادة كتابة `src/lib/auth.ts:104-113`
- `User`: إضافة `branch` · `reportsTo` · `isProducer` · `lastLogin`
- **نطاق الرؤية الثلاثي** (الكل / الفريق عبر `reportsTo` / الذات) في طبقة الخادم
- جدول `ListItem` + نقل كل القوائم من `src/lib/constants.ts` إليه + شاشة إدارتها
- جدول `Setting` مفعَّلًا بمفاتيح §٩ + شاشة إعدادات
- `AuditLog` كامل (الحقل، القيمة القديمة، الجديدة) + الحذف المنطقي
- الأدوار السبعة والمصفوفة الافتراضية من §٥ كبيانات ابتدائية

**اختبارات:** ١–٧ من §١٧ (كلها صلاحيات).

---

## المرحلة ٢ — العملاء والليدز والبحث بالهاتف

**لماذا:** أول قيمة يلمسها المستخدم يوميًا، وتحسم مشكلة تكرار العملاء.

- `Client` موحّد (فرد/شركة) + `phoneNormalized` + قاعدة التطبيع السباعية (§١٤)
- **منع الازدواج على مستوى الهاتف** — قيد فريد لا فحص واجهة
- بحث فوري بالهاتف **≤٣٠٠ ملّي ثانية** + بطاقة العميل (مشاريعه، إجمالي مشترياته، آخر تعامل)
- شاشة **«ليد جديد» بثلاثة حقول ≤١٥ ثانية** — الهاتف أولًا
- `Lead`: `firstReplyAt` · `lossReason` إلزامي عند الخسارة · `channel` · `contactMethod` · `branch` · `estPages`
- معرّفات تسلسلية `CL-00001` و`LD-2608-0001`

**اختبارات:** ٢٠ (لا عميل مكرر) · ٢١ (سرعة البحث) · معيار ≤١٥ث.

---

## المرحلة ٣ — المشاريع والتحويل والحالات

**لماذا:** الكائن المحوري الذي تدور حوله كل الحسابات.

- تحويل `Deal` → `Project` بكل حقول §4.3
- الحالات التشغيلية الثمانية + قواعد الانتقال (§٦)
- شاشة **«تحويل ليد → مشروع» ≤٢٠ ثانية** بستة حقول + زر «فوري»
- `deposit` / `collected_amount` / `balance` / الطوابع الزمنية الخمسة
- **حقل `alert`** بترتيب أسبابه التسعة
- إعادة استخدام لوحة السحب والإفلات لحالات المشروع

**اختبارات:** ١٣ (الملغى خارج المالي) · ١٤ (قيد التنفيذ لا يُحتسب).

---

## المرحلة ٤ — الإسناد والخطوات ومعادلات التكلفة

**لماذا:** تفتح كل الحسابات، وهي قلب المشروع.

- `StaffCost` + `ProjectStep` + `workMode` / `sourcing` / `primaryProducer` / `reviewer`
- **معادلات §٨ حرفيًا:** `weighted_pages` بالحد الأدنى · التكلفة الداخلية
  والخارجية · تكلفة المراجع مع **منع الاحتساب المزدوج** · الهامش · الإيراد المنسوب
- شاشة **«قائمة الإسناد» بلا سعر بيع** + شاشة إسناد ≤١٥ث
- **مؤشر التكلفة المجرّد** (رقمان بلا أي إشارة لراتب)
- شاشة التسليم + `qaIssues`

**اختبارات:** ٨ · ٩ · ١٠ · ١١ · ١٢ · ١٦ (كلها منطق عمل حسابي).

---

## المرحلة ٥ — التسعير والخصومات والاعتماد

- `PriceListItem` بـ`effectiveFrom` و`minOrder` + شاشتها
- تسعير تلقائي عند التحويل · رسم الاستعجال · الحد الأدنى للطلب
- أنواع الخصم الأربعة + **خصم العميل المتكرر تلقائيًا**
- حدود الخصم بالدور + `approvalState` + **منع الانتقال حتى الاعتماد**

**اختبارات:** ١٨ (الحجب حتى الاعتماد) · ١٩ (تغيير السعر لا يمسّ الماضي).

---

## المرحلة ٦ — نظام نسب المبيعات ⭐ (مُقدَّم بناءً على طلبك)

**لماذا هنا لا في الآخر:** يحتاج المشروع والتسعير والتحصيل (٣ و٥) وحدها. وتقديمه
يجعل فريق المبيعات **يرى مكسبه من أول شهر** — وهذا أقوى دافع تبنٍّ في المنظومة
كلها، وهو الخطر الأول المذكور في خاتمة المواصفة.

- `CommissionScheme` + `CommissionSchemeTier` (الشرائح) + `CommissionAssignment`
  (من يخضع لأي خطة ومن متى) + `CommissionEntry` (الاستحقاق المحسوب)
- شاشة إعداد الخطط: الشرائح، الأساس، التقسيم بين الأدمن ومديره
- **شاشة «نسبي»** للبائع: المحقق هذا الشهر · الشريحة الحالية · **كم يتبقى للشريحة
  التالية** · تفصيل بالمشروع
- شاشة للمدير: نسب فريقه وحصته
- الاحتساب **مشتق ويُعاد بناؤه** — لا إدخال يدوي، وتغيير الخطة لا يمسّ شهورًا مغلقة

> ⚠️ **محجوبة على إجاباتك في §٧ من `GAP-ANALYSIS.md`.** الأسئلة الستة تحدد
> المعادلة نفسها، ولن أخترعها.

---

## المرحلة ٧ — الفريلانسرز

- `Freelancer` + `FreelancerRate` (الاستثناءات) + `FreelancerPayment`
- **محرّك اختيار يتحمّل ١٠٠٠ اسم:** ترشيح مسبق بزوج اللغة وخط الخدمة ·
  فهرس خفيف في المتصفح · بحث **≤٣٠٠ ملّي ثانية** بلا نداء لكل حرف
- الترتيب الخماسي (معتمد ← الأرخص ← الأعلى تقييمًا ← الأكثر عملًا ← الأحدث)
- استيراد جماعي من ملف + سطر استحقاق تلقائي عند الإسناد

**اختبارات:** ٢١ (سرعة البحث بين ١٠٠٠).

---

## المرحلة ٨ — الماليات

مقدمات محتجزة · مستحقات العملاء **بأعمارها** · تحصيلات الشهر ·
مستحقات الفريلانسرز مع «تأكيد الإرسال» · تصدير.

---

## المرحلة ٩ — التحليلات

تحليلات §١٣ الخمس، وعلى رأسها **«هامش كل نمط تشغيل»** بالتكلفة المستوعبة —
وهو السؤال الاستراتيجي الذي تصفه المواصفة بأنه سبب المشروع.

**اختبارات:** ١٧ (مقام الالتزام بالمواعيد) · ٢٢ (لوحة ٥٠٠٠ مشروع ≤٢ث).

---

## المرحلة ١٠ — التنبيهات

جدول §١٢ الثمانية، **مجمَّعة برسالة واحدة لكل شخص لكل حدث**.

---

## المرحلة ١١ — ترحيل البيانات القديمة

آخر شيء (§١٨ بند ١٠) بعد استقرار السكيما. المصادر الثلاثة بعيوبها الموثّقة،
وجدول `migration_review` لكل سجل لا يُحسم.

**اختبارات:** ٢٣ · ٢٤ · ٢٥ · ٢٦.

---

## ما يبقى من النظام الحالي

طبقة المصادقة والجلسات · نمط الحفظ الموثوق (POST→تحويل) · الواجهة العربية
ومكوّناتها · النشر التلقائي والفحص الآلي · شاشات المهام والملاحظات وعروض الأسعار
(بتعديل) · لوحة السحب والإفلات.

## الترتيب النهائي المقترح

```
١ الأساس  →  ٢ العملاء والليدز  →  ٣ المشاريع  →  ٤ الإسناد والتكلفة
                                                          ↓
٨ الماليات  ←  ٧ الفريلانسرز  ←  ٦ النسب ⭐  ←  ٥ التسعير
     ↓
٩ التحليلات  →  ١٠ التنبيهات  →  ١١ الترحيل
```

---

## قبل البدء — أحتاج منك

1. **اعتماد هذه الخطة** أو تعديل ترتيبها
2. **إجابات الأسئلة التسعة** في §٧ من `GAP-ANALYSIS.md` — الستة الأولى تحجب
   المرحلة ٦، والثلاثة الأخيرة تحجب المرحلتين ١ و٣
3. تأكيد قرارات التعارض الثلاثة في §٥ من `GAP-ANALYSIS.md`

> يكفي أن تجيب أسئلة المرحلة ١ الآن (٧ و٨ و٩) لأبدأ فورًا، وتُجيب أسئلة النسب
> قبل وصولنا للمرحلة ٦.
