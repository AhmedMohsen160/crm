# المرحلة ١١ — الباك إند والبريد والذكاء الاصطناعي والهوية

> طلب الإدارة في هذه المرحلة سبعة بنود. هذا ما نُفِّذ في كل بند، وأين يقع في الكود،
> وما بقي مفتوحًا عمدًا.

---

## ما طُلب وما سُلّم

| # | الطلب | الحال | المكان |
|---|---|---|---|
| ١ | مراجعة الباك إند وترتيب متطلباته | ✅ | `docs/ARCHITECTURE.md` §١ |
| ٢ | أقوى طريقة لحفظ الداتا وتنظيمها | ✅ | `docs/ARCHITECTURE.md` §٢ |
| ٣ | تقسيم النظام فلا يُعاد بناؤه مع كل طلب | ✅ | `src/lib/mutations/*` — ١٢ وحدة |
| ٤ | دمج البريد: قراءة وتحليل وردّ | ✅ | `email.ts` · `email-engine.ts` · `/email` |
| ٥ | API للذكاء الاصطناعي للمساعدة من الداخل | ✅ | `ai.ts` · `ai-client.ts` · `assistant.ts` · `/assistant` |
| ٦ | API لعروض أسعار احترافية كالمرفق | ✅ | `proposals.ts` · `/proposals` · صفحة الطباعة |
| ٧ | هوية فاست ترانس في الفرونت إند | ✅ | `brand.tsx` · `tailwind.config.ts` |

---

## ١ · تقسيم النظام — الجواب عن «لا أريد إعادة بناء»

**كان**: ملف واحد `src/lib/mutations.ts` بـ**٢٧٢١ سطرًا** يحوي عمليات الحفظ كلّها —
الليدز والمحاسبة والفريلانسرز والمشاريع والنِّسَب في مكان واحد. أي طلب تعديل يفتح
الملف نفسه، فتعديل في الفريلانسرز يخاطر بكسر المحاسبة.

**صار**: ١٢ وحدة بالمجال + `base.ts` للأدوات المشتركة + `index.ts` واجهةً تُصدّر
الجميع. لم يتغيّر سطر واحد في أي مُستدعٍ: الاستيراد لا يزال من `@/lib/mutations`.

```
mutations/  base 39 · ai 53 · alerts 116 · pricing 128 · tasks 134
            migration 157 · proposals 188 · commissions 219 · email 257
            freelancers 383 · admin 477 · accounting 513
            projects 629 · sales 636
```

**الأثر عمليًّا**: «أضف حقلًا للفريلانسر» تفتح `mutations/freelancers.ts` وشاشته
والمخطّط — ولا تُلمس المحاسبة ولا الليدز ولا المشاريع ولا النِّسَب.

الجدول الكامل لما يُفتح مع كل نوع طلب في `docs/ARCHITECTURE.md` §٤٫٢.

---

## ٢ · البريد

### النمطان معًا

| النمط | الضبط | من يقرؤه |
|---|---|---|
| صندوق مشترك للفريق | «صاحب الصندوق» فارغًا | كل من يملك `canUseEmail` |
| صندوق لكل أدمن | اختيار الموظف | صاحبه، ومن يملك رؤية كل السجلات |

يمكن تشغيل النمطين في وقت واحد: صندوق `info@` مشترك، وصندوق لكل أدمن بجانبه.

### المسار

```
IMAP ← حفظ الرسالة عندنا ← ربطها بسجلّ ← تصنيف ← تلخيص ← مسوّدة ردّ ← إرسال بيد الموظف
```

**الربط الآلي** بثلاث محاولات مرتّبة: بعنوان البريد (الأدقّ)، ثم بأي هاتف مذكور في
نصّ الرسالة، ثم بنطاق الشركة. والنطاق العام (`gmail.com` وأمثاله) **لا يُربط به شيء**
— وإلا صار كل عملاء جيميل شركةً واحدة.

### قراران ملزمان

1. **لا يُرسَل ردّ من تلقاء النظام.** النموذج يقترح، والموظف يقرأ ويعدّل ثم يضغط
   إرسال. الرسالة الصادرة باسم المكتب التزامٌ عليه، ولا يجوز أن يُلزمه ما لم يقرأه
   إنسان.
2. **نصّ الرسالة يُحفظ عندنا كاملًا.** صندوق البريد يُمسح منه، والمراسلة مع العميل
   جزء من ملفّه لا من الصندوق.

### الأمان

كلمة مرور الصندوق تُعمّى بـ**AES‑256‑GCM** بمفتاح **خارج قاعدة البيانات**
(`SECRET_KEY`، أو `AUTH_SECRET` إن لم يُفرَد). تسريب نسخة من القاعدة وحده لا يسلّم
بريد المكتب. ولا تُعرض المحفوظة في أي شاشة، وتُترك فارغة عند التعديل لتبقى كما هي.

---

## ٣ · الذكاء الاصطناعي

### ثلاثة استعمالات

| الاستعمال | ما يفعله | السقف |
|---|---|---|
| تحليل الرسالة | نية · إلحاحية · نبرة · ملخّص · مسوّدة | ١٢٠٠ رمز |
| صياغة ردّ | بنبرة مختارة وبتوجيه الموظف | ١٥٠٠ رمز |
| المساعد | يجيب عن أرقام النظام | ٢٠٠٠ رمز |

### ثلاثة قيود مبنيّة في الكود

1. **المساعد لا يرى إلا ما يراه سائله.** سياقه يُبنى بصلاحياته هو، فلا يجيب مدير
   المشاريع عن سعر البيع المحجوب عنه في §٥. ولو بُني السياق بصلاحيات الخادم لصار
   المساعد بابًا خلفيًّا لكل ما أُغلق في الشاشات.
2. **لا يخترع رقمًا.** ما لم يُعطَ له يكتب مكانه `[يُملأ من النظام]`، ولا يَعِد
   بموعد ولا بسعر ولا بخصم.
3. **كل نداء مسجَّل ومحدود.** `AiRun` يحمل الرموز والتكلفة والزمن، و`AI_DAILY_BUDGET_USD`
   يوقف عند سقف اليوم.

وحين ينقطع المفتاح **يظلّ النظام يعمل**: التصنيف يسقط للتخمين النصّي، والرسالة
تُحفظ وتُربط وتُصنَّف بلا نموذج، والأزرار الذكية تختفي وحدها.

### حجب البيانات

الهواتف والأرقام البنكية تُستبدل برموز قبل مغادرة النظام: معناها لا يلزم للتحليل،
ورقمٌ يغادر لا يعود.

---

## ٤ · العروض الاحترافية

مستند يُرسَل للعميل المؤسسي — لا سطر أسعار لطلب واحد. يحمل مرجعًا ومراجعةً ومدة
صلاحية وموقِّعًا، ويُطبع بهوية المكتب في سبعة أقسام كالمرفق.

**أخطر قاعدة فيه**: الشريحة الكمّية **بأثر رجعي على حجم الشهر كاملًا** — وهي عكس
شرائح النِّسَب في §٦ تمامًا. ٢٠٠ ألف كلمة تُحاسَب بـ**١٤٤٠٠ ريال** (الشريحة الواحدة
على الكل)، لا ١٥٢٠٠ (تدرّجًا على الأجزاء). اختبارٌ يثبّت الرقمين معًا حتى لا يُخلط
بينهما مستقبلًا.

والمراجعة الجديدة **تُنسخ ولا تستبدل الأصل**: العميل قد يكون قرأ الأول، فمحوُه يمحو
ما بنى عليه قراره.

**قبول العرض لا يفتح مشروعًا تلقائيًّا** — مقصود: العرض إطار سعري لشهور، والمشروع
يُفتح بدفعة فعلية.

---

## ٥ · هوية فاست ترانس

الألوان مستخرَجة من ملف العرض الأصلي لا مقدَّرة بالعين:

| اللون | القيمة | أين |
|---|---|---|
| كحلي | `#242E5B` | `brand-600` — القوائم والعناوين والغلاف |
| ليموني | `#B9D716` | `lime-400` — التأكيد والحالات الموجبة |

الشعار مرسوم SVG في `src/components/brand.tsx` (لا صورة تُحمَّل)، ويظهر في: الشريط
الجانبي، وشريط الجوال، وشاشة الدخول، وأيقونة التبويب، وغلاف العرض المطبوع.

---

## ٦ · صلاحيتان جديدتان

| المفتاح | ماذا يفتح |
|---|---|
| `canUseEmail` | قراءة صناديق البريد المتاحة والرد عليها |
| `canUseAi` | المساعد وصياغة الردود — بحدود ما يراه صاحبها |

صارت **٢١ صلاحية**. وشاشة الأدوار تلتقطهما آليًّا لأنها مبنيّة من `PERMISSION_KEYS`.
تشغيلهما على قاعدة قائمة بأمر واحد: `npm run bootstrap`.

---

## ٧ · متغيّرات البيئة الجديدة

| المتغيّر | لازم؟ | بلا ضبطه |
|---|---|---|
| `SECRET_KEY` | للبريد | يُشتقّ من `AUTH_SECRET`؛ ويُرفض حفظ الصناديق دونهما |
| `ANTHROPIC_API_KEY` | للذكاء | الأزرار الذكية تختفي والباقي يعمل |
| `AI_MODEL` · `AI_DAILY_BUDGET_USD` | لا | الافتراضي نموذج متوازن وسقف ٥$ يوميًّا |
| `CRON_SECRET` | للجدولة | `/api/cron` **مغلق** — الافتراض الآمن |

---

## ٨ · الاختبارات

| الوحدة | العدد |
|---|---|
| `test:email` | **٧٦** جديدة |
| `test:ai` | **٤٩** جديدة |
| باقي الوحدات | ٤٨٠ |
| **مجموع اختبارات الوحدة** | **٦٠٥** |
| `test:e2e` في كروميوم حقيقي | **١٦٨** (٧ منها جديدة) |

**ثلاثة أخطاء حقيقية** كشفتها اختبارات البريد قبل أن يراها أحد:

1. فاصل الاقتباس العربي «في ٣ أغسطس، **كتب فلان**:» لم يكن يُنزع، لأن النمط كان
   يتوقّع النقطتين بعد الفعل مباشرةً — فكان نصّ الرسالة القديمة يدخل التحليل.
2. التوقيع العربي كان يبقى، لأن `\b` في جافاسكربت **لاتينيّ صرف** فلا يقع بعد حرف
   عربي.
3. تحويل HTML كان يترك مسافةً في أول كل سطر.

---

## ٩ · ما بقي مفتوحًا عمدًا

| البند | لماذا |
|---|---|
| تنزيل مرفقات البريد | تُسجَّل أسماؤها؛ وتنزيلها يحتاج قرارًا في مكان التخزين وحدّ حجمه |
| البحث النصّي في أرشيف البريد | `contains` يكفي الآن؛ يُرقّى لفهرس نصّي عند عشرات الآلاف |
| قبول العرض لا يفتح مشروعًا | قرار مقصود لا نقص |

---

## ⚠ البند الأخطر — ما زال ينتظر الإدارة

كلمة مرور قاعدة الإنتاج ظهرت في لقطة شاشة سابقة، والقاعدة تحمل الآن **٣٤٤٨ اسم
عميل حقيقي مع أرقام هواتفهم**.

**الخطوات**: Neon ← Reset password ← تحديث `DATABASE_URL` في Vercel ← Redeploy.

لا شيء في هذه المرحلة ولا في التي قبلها يُغني عن هذه الخطوة.
