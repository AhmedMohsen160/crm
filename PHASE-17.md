# المرحلة ١٧ — تسليم الجلسة

> **هذا الملف يُقرأ في أول جلسة قادمة بعد `CLAUDE.md`.** فيه ما أُنجز في هذه
> الجلسة، وما بقي مفتوحًا، وما ينتظر قرارًا من أحمد، وما عليه هو أن يفعله في
> لوحة النشر.
>
> من الكوميت `1f7d2ab` إلى `96c311c`.

---

## ١ · ما أُنجز — بالترتيب

### أ · إصلاحات جاءت من تجربة أحمد الفعلية

| ما اشتكى منه | السبب الجذري | ما صار |
|---|---|---|
| «اعتماد وترحيل» يُعيد بناء الصفحة كلها | نموذج POST ثم تحويل ٣٠٣ | `json=1` على نفس بوابة `/api/save` — الصفّ يتبدّل في مكانه، والنموذج تحته يبقى POST عاديًا فيعمل بلا جافاسكربت |
| مدير المبيعات لا يجد عملاءه | الترشيح على `Client.ownerId` وهو **من كتب البطاقة**، والبيانات مستوردة بحساب واحد | `teamClients`: النطاق **من أتمّ البيع** — اتحاد من له مشروع يملكه الفريق ومن أنشأ الفريق بطاقته |
| «سُلّم» صفرٌ في اللوحة والمشروع في «المشاريع» | نطاق اللوحة كان `ownerId: user.id` ونطاق «المشاريع» نطاقَ فريق | اللوحة تستعمل `ownerFilter` نفسه |
| لا تنبيه عند التسليم | الحدث «فوريّ» لكن الفوريّ لا يُطلق إلا بالمجدوِل، و`/api/cron` مغلق | `deliverProject` يُطلق الحدث بنفسه — لصاحب المشروع **ولمديره** |
| «المنتِج بلا تكلفة» ورواتبهم مُدخَلة | التكلفة تُقرأ من `StaffCost` والرواتب في الموارد البشرية — مخزنان لرقم واحد | `staffRateOf` يشتقّ الأجر من بنود الأجر السارية؛ و`StaffCost` للنسبة والطاقة فقط |
| مدير المشاريع يرى مبيعات غيره | دوره كان يحمل `canViewAllLeads` ليصل إلى قائمة الإسناد | `projectFilter` من `canAssignProduction`، والصلاحية الواسعة سقطت |
| لوحة الترتيب تعرض من هم خارج الفريق | لا نطاق عليها | مقيَّدة بنطاق الرؤية إلا لمن يملك تحليلات الشركة أو دفاترها |

### ب · ما بُني جديدًا

- **لوحة تشغيل لمدير المشاريع** (`ops-dashboard.tsx`): الالتزام بالموعد، الحجم، التقدّم عن الشهر الماضي، تكلفة التشغيل وعائدها ونسبتها من المبيعات، حجم كل منفِّذ، وما ينتظر الإسناد. والشرط صلاحيةٌ لا اسمُ دور: `canAssignProduction && !canViewSellPrice`.
- **سجل العملاء**: تصنيف (متكرّر · جديد · متوقّف · بلا تعامل) وفلاتر بالأدمن والفترة والنوع والفرع، وبحث فوريّ، وزر «مشروع جديد» لكل صف.
- **زر التحصيل من الصف** — بالمتبقي مكتوبًا سلفًا. و`collectNow` **دفعةُ اليوم** والخادم يجمعها على ما سبق.
- **قياس الجودة**: كثافة الملاحظات لكل ألف كلمة (SAE J2450 · LISA QA · MQM) + نسبة الإعادة. والوحدة واحدة في لوحة التشغيل و«أدائي».
- **حدُّ محاولات الدخول**: عشر في ربع ساعة، والعدّ في القاعدة.
- **النسخة الاحتياطية**: `/settings/backup` — ٣٨ جدولًا بلا أسرار.
- **سجل الأعطال**: `/settings/errors` — يُنقّى قبل الكتابة، وبطاقة واحدة لكل عطل متكرّر.
- **ملفات الترحيل**: `0_init` + `scripts/migrate.mjs` (يُهيّئ نفسه) + `migration:new` + `check:migrations`.
- **حساب المال بالقرش**: `money.ts` + `check:money` — فُحص الدفتر الحقيقي فتوازن بالقرش.
- **المهام المجدولة**: كل مهمة نداءٌ بجدوله في `vercel.json`، وميزانية وقت، وشاشة `/settings/jobs`.
- **تكلفة الليد والعميل المكتسب**: من الدفتر لا من إدخال ثانٍ.

**النتيجة: ١٬٢٩٣ اختبار وحدة + ٢٨٢ e2e — كلها ناجحة.**

---

## ٢ · ما ينتظر أحمد في لوحة النشر

| # | الإجراء | لماذا |
|---|---|---|
| ١ | ضبط `CRON_SECRET` في متغيّرات البيئة ثم Redeploy | بدونه المهام المجدولة **لا تعمل** — لا تنبيهات ولا سحب بريد |
| ٢ | تنزيل نسخة احتياطية أسبوعيًّا والاحتفاظ بأربع | من `/settings/backup` |

**ولا خطوة يدوية للترحيل** — أول نشرة تُهيّئ القاعدة بنفسها.

---

## ٣ · ما بقي مفتوحًا

### أ · بوابة العميل — **مُقرَّرة ولم تُبنَ**

قرار أحمد النهائي: **الخيار (أ) — رابط سرّي طويل بلا كلمة مرور.**

المتطلبات كما ذكرها حرفيًّا:
- **اختيارية لكل مشروع** — تُفتح وتُغلق من بطاقة المشروع، فهي تناسب بعض العملاء وبعض المشاريع لا كلّها
- الرابط **نرسله نحن** للعميل
- يتابع **تقدّم العمل** في مشروعه
- **يحمّل الملف النهائي** من البوابة عند انتهائه

التصميم المقترح (لم يُنفَّذ بعد):
- عمود `portalToken String? @unique` و`portalOpenedAt` على `Project` — **إضافيّان قابلان للفراغ**
- مسار عامّ خارج `(app)` بلا حارس جلسة: `/p/[token]`
- يعرض: اسم العميل · رقم المشروع وعنوانه · الحالة بالعربية · الموعد · شريط تقدّم من `PROJECT_FLOW`
- **لا يعرض**: سعرًا ولا تكلفة ولا اسم منفِّذ ولا أي مشروع آخر
- زر التحميل **يظهر بعد `delivered` فقط**
- إلغاء الرابط بضغطة من بطاقة المشروع يُبطل الوصول فورًا
- الرابط يدخل `middleware` كمسار عامّ مثل `/login`

### ب · تصنيف المصروف: مركزيّ أم خاصّ بفرع — **موجود بالفعل، لا يُبنى**

سأل أحمد عنه في آخر رسالة، والجواب أن النظام يفعله اليوم:
- `JournalLine.branch` و`costCenterId` — حقلان في **كل سطر مصروف** في شاشة القيد
- فئات مراكز التكلفة في `branch-economics.ts`: `branch_direct` (ذاتيّ للفرع) مقابل `central_marketing` و`production` و`general_admin` (الكتلة المشتركة)
- و`checkClassification` **يفرض** القاعدتين: بندٌ مشترك **لا يحمل فرعًا**، وبندٌ ذاتيّ **يلزمه فرع**
- و`suggestAdKind` يفصل إعلانَ منطقةِ الفرع عن تسويق العلامة (SEO والمحتوى) — وهما مثالا أحمد حرفيًّا

**ما قد يحتاج عملًا**: التحقّق مع أحمد أن الشاشة تسأله عن هذا بوضوح كافٍ عند إدخال المصروف، وأن التحليلات تعرض المصروف مقسومًا (فرعيّ مقابل مركزيّ) — لا التصنيف نفسه.

### ج · مؤجَّل بقرار أحمد الصريح

- تسجيل الوقت الفعلي — **لا نحتاجه**
- تخزين الملفات السحابي — **لا نحتاجه** حتى إشعار آخر
- تحويل نوع عمود المال إلى `Decimal` — الحساب صار بالقرش والخطر خُفّض؛ التحويل مرحلة مستقلة بترحيل بيانات

### د · قراران قديمان ما زالا معلَّقين (من المرحلة ١٥)

1. هل يُقرأ تصنيف توزيع المصروف **من الحساب** بدل مركز التكلفة؟ (الدليل: ٧٥٤٬٨٠٤ ج من مصروف الإنتاج كلها على المقطم)
2. ما الأساس الذي يُضرب فيه مُعامل التارجت ١٫٢٠ — المصاريف المباشرة للفرع وحدها أم ومعها الكتلة المركزية الموزَّعة؟

---

## ٤ · أخطاء في دفاتر أحمد لم تُحسم

- **عدم اتزان ٣٥٬٨٣٥ ج في دفتر يوليو ٢٠٢٦** — المستورد يرفض إجباره، والخطأ في ورقة المحاسب لا في النظام.

---

## ٥ · ملاحظات تشغيلية للجلسة القادمة

- **الفرع**: `claude/crm-sales-team-system-1f54m8` — و`main` يُزامَن معه بعد كل كوميت، فالدومين `www.fastrans-crm.com` يعرض الأحدث تلقائيًّا.
- **قبل كل كوميت**: `npx tsc --noEmit` · `npm run test:unit` · `npm run build` · `npm run test:e2e`.
- **اختبارات e2e تتراكم بياناتها** في قاعدة التطوير: كل تشغيل يُنشئ فريلانسر ومشاريع. وقد أسقط ذلك اختبارًا مرة — فالبحث في الاختبارات يكون **بالجزء الفريد** من الاسم لا ببادئته.
- **أحمد لا يقرأ الأسهم** `←` `→` في الخطوات: تنقلب في العربية. الخطوة رقمٌ في سطر، والكلمة الإنجليزية وحدها في آخر الجملة.
